# Testing

- Shared unit tests: plate normalization, task generation, expense totals
- API unit tests: ministry record mapping
- Web tests: empty dashboard (no fabricated expenses)

Run `npm test` from the repo root.
